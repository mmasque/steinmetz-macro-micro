function weigthedCorrMatrix = weighByDistance(corrMatrix, distances)
    %distances should be an array where distances(i) is the distance of
    %corrMatrix(i,i) element to the tip of probe. 
    
end